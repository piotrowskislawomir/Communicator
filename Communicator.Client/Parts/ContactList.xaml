<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"             
             xmlns:ViewModels="clr-namespace:Communicator.Client.ViewModels" 
             x:Class="Communicator.Client.ViewModels.ContactList"              
             mc:Ignorable="d" d:DesignWidth="393.308" d:DesignHeight="274.887" >
    <UserControl.DataContext>
        <ViewModels:ContactViewModel/>
    </UserControl.DataContext>
    <Grid>
        <ListView HorizontalAlignment="Left" Height="190" Margin="27,40,0,0" VerticalAlignment="Top" Width="350" ItemsSource="{Binding ContactList}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Background="Bisque">
                        <TextBlock Text="{Binding Name}"/><TextBox Text="{Binding Nick}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</UserControl>
