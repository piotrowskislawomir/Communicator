<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"             
             xmlns:ViewModels="clr-namespace:Communicator.Client.ViewModels" 
             x:Class="Communicator.Client.ViewModels.ContactList"              
             mc:Ignorable="d" d:DesignWidth="393.308" d:DesignHeight="274.887" >
    <UserControl.DataContext>
        <ViewModels:ContactViewModel/>
    </UserControl.DataContext>
    <Grid>
        <ListView Background="{x:Null}" HorizontalAlignment="Left" Height="200" VerticalAlignment="Top" Width="230" ItemsSource="{Binding ContactList}" BorderThickness="0" Margin="20,30,0,0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Background="{x:Null}">
                        <TextBlock Style="{StaticResource TextBloack_ContactName}" Text="{Binding Name}"/>
                        <TextBox Style="{StaticResource PART_TextBox_Metro}" Text="{Binding Nick}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</UserControl>
